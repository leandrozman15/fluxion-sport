
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas globales para prototipado rápido
    // Versión: 1.0.1 - Forzando despliegue para subcolecciones de jugadores
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
