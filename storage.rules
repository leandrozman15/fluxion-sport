
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // PERMISOS TOTALES PARA PROTOTIPADO Y DESARROLLO
      // Permite la gestión de imágenes de clubes, jugadores y tienda sin restricciones.
      allow read, write: if true;
    }
  }
}
