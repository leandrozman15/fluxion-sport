
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Lectura pública para imágenes de clubes, jugadores y tienda
      allow read: if true;
      // Escritura solo para usuarios autenticados
      allow write: if request.auth != null;
    }
  }
}
